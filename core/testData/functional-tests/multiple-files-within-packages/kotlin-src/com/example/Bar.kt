package com.example

/**
 * <!--dtests-->
 * Returns a string of length 3:
 * ```
 * assertEquals(3, bar().length)
 * ```
 * It starts with b:
 * ```
 * assertTrue(bar().startsWith("b"))
 * ```
 */
fun bar(): String {
    return "bar"
}